# servidor/.env.example
# Variables que el servidor Node podría leer directamente si no se pasan por Docker Compose
# (En nuestra configuración actual, las importantes se pasan desde el .env de la raíz)

DATABASE_URL= # Ejemplo: mongodb://localhost:27017/liga_local (para correr SIN Docker)
PORT=4000
FRONTEND_URL= # Ejemplo: http://localhost:5173 (para correr SIN Docker)
TOKEN_SECRET=
